// Token Generator
// Create multiple tokens

fn execute_ability() {
    [
        create_token(controller, "soldier_token", "field"),
        create_token(controller, "soldier_token", "field")
    ]
}
