// Power Surge
// Pump and add counters
// NOTE: add_counter not yet implemented in executor

fn execute_ability() {
    [
        pump_creature(source_card, 2, 2)  // Validates but doesn't execute yet
        // add_counter(source_card, "+1/+1", 2)  // Not yet implemented
    ]
}
